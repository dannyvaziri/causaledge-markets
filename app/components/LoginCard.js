'use client';
export default function LoginCard({loading,error,draftToken='',setDraftToken=()=>{},unlock=()=>{}}){if(loading)return <main className="loginShell"><section className="loginCard"><div className="hexMark big">CE</div><h1>CausalEdge Markets</h1><p>Authenticating command center…</p></section></main>;return <main className="loginShell"><section className="loginCard"><div className="hexMark big">CE</div><p className="eyebrow">GLOBAL MARKET INTELLIGENCE</p><h1>CausalEdge Markets Command Center</h1><p>Live global intelligence, market monitoring, AI dossiers, and autonomous paper trading under deterministic risk controls.</p>{error&&<p className="loginError">{error}</p>}<button className="googleBtn" onClick={()=>{window.location.href='/api/auth/google/start'}}>Continue with Google</button><div className="loginDivider"><span>OR</span></div><form onSubmit={unlock}><label>Private dashboard password</label><input type="password" value={draftToken} onChange={e=>setDraftToken(e.target.value)} placeholder="Enter dashboard password" autoComplete="current-password"/><button type="submit">Open command center</button></form><small>Live-money execution is not enabled. Trading controls route to the isolated Alpaca paper environment.</small></section></main>}
